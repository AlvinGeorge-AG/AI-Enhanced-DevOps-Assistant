events {}
http {
    upstream app_backend {
        # Using Docker's internal DNS to point to the base app service
        server app:5000; 
    }
    server {
        listen 80;
        location / {
            proxy_pass http://app_backend;
        }
    }
}