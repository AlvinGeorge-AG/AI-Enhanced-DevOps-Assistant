# Load balancer config (modified later by Module 4)
events {}

http {
    upstream app_backend {
        server app:5000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://app_backend;
        }
    }
}
