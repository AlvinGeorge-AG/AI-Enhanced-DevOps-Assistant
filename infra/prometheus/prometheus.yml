# Tells Prometheus what to scrape
global:
  scrape_interval: 5s

rule_files:
  - 'alert_rules.yml'

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']

scrape_configs:
  - job_name: 'target_app'
    static_configs:
      - targets: ['app:5000']
